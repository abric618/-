欧几里得算法(也就是辗转相除法)有普通的和扩展两种，普通的不是本篇重点，只附上实现的代码(python语法)：
a=int(input())
b=int(input())  #输入a和b的值
if a>b:  #大小转换，看着舒服
  temp=a
  a=b
  b=temp
while a!=0: #开始循环
  temp=a
  a=b%a
  b=temp
print(b)
那么扩展欧几里得算法是解决什么问题的呢？也就是为什么会有扩展欧几里得算法呢？扩展欧几里得算法主要是用来解决一类非常经典的数学问题：
给定两个整数a和b,不仅能求出它们的最大公约数gcd(a,b),还能同时找出一对整数x和y,，使得下面的等式成立：
                                    a*x+b*y=gcd(a,b)
这个等式被称为贝祖等式。所以扩展欧几里得算法正是在普通辗转相除法的基础上，增加了“求出系数x和y”的功能。
由于RSA双密钥算法的提出，扩展欧几里得算法就体现在了求乘法逆元的应用上。我接触这个算法就是为了RSA，在学习的过程中，我很容易理解这个算法的原理，但是对代码的实现
一头雾水，不得窍门，经过一阵子的思索过后，我终于搞懂了该怎么在代码上实现。我一直希望扩展欧几里得算法能够在普通欧几里得算法的基础上去实现，因为我对普通欧几里得
算法还是很熟悉的，最后还是掌握了该怎么去做。我归纳之后得出了这个算法的精髓：本步的系数=上上步的系数-本步的商*上一步的系数。下面是代码实现(python)语法：
e=int(input())
fn=int(input())
if e>fn:
  temp=e
  e=fn
  fn=temp
a=e
b=fn
t=0 #上上步的系数
newt=1  #上一步的系数
while a!=1：
  temp=a
  k=b//a  #地板除法，和/不同，本步的商
  a=b%a
  b=temp  #普通欧几里得到此
  tmep=newt
  newt=t-k*newt #本步的系数
  t=temp
if newt<0:
  newt+=fn
print(newt)
在理解这个算法的过程中，我也领悟到了把算法在代码层面上实现的一点就是要找到通式，找到通式之后就好写代码了
