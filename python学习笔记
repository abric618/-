1，python换行：字串换行用\n
2,列表：列表可以用-1，-2等负数索引来调用最后位置的元素
        方法：list.append(content):用来在列表的末尾添加一个新元素,这个content可以用f来自定义格式
              list.sort(content,key):直接在原列表上按key排序，返回列表
              list.reverse():原地反转list
3，类和对象：
4，字典：尾随逗号可写可不写
        字典的键如果没有加引号，就会变成变量，在调用时Python会认为它是一个变量名，然后去内存里找这个变量，用它的值作为键。加上引号就会变成字串
        方法：dict.keys():以列表的方式返回dict的所有键，只读
              dict.values():以列表的方式返回dict的所有值，只读
              dict.items():以元组的方式返回dict的所有键值对
5,for的使用：在python中，for搭配上range(),切片和enumerate()会让它和C语言里面的for一样好用
6，字串：
        方法：str.join(obj):把可迭代对象以str为连接符合并在一起
              str.lstrip('ch'):只去掉str左边的ch
              str.rstrip('ch'):只去掉str右边的ch
              str.strip('ch'):从左到右去除str的ch
7，内置函数：不用导入模块，不用点来引用
            len(obj):返回可迭代对象的长度
            range(start,stop,step):返回一个从start到stop，步长为step的整数序列，当step=-1时可以反转
            enumerate(obj,start):给可迭代对象编号
            eval(input()):智能读取输入内容
            type(obj):返回obj的类型
            isinstance(obj,tyoe):若obj的类型和type相同，则返回True,否则返回False
            reversed(obj):返回一个倒序obj的列表
            sorted(obj,key):返回一个按key排序的新的可迭代对象
            abs(a):计算a的绝对值
8，切片：可迭代对象均可
9，集合：无序，不重复的可迭代对象
10，模块：math模块：sqrt():计算平方根
                   dist(a,b):计算a和b两点之间的欧几里得距离，需要把a和b的坐标打包成列表或者元组
                   hypot(a,b):等价于sqrt(a*a+b*b)
        statistics模块：mean(obj):返回obj的平均数
                        mutimode(obj):返回obj的众数，如果存在多个，则返回一个列表
        ramdom模块：ramdom():生成一个0到1的浮点数
                   uniform(a,b):生成一个a到b但不包括a和b的浮点数
                   randint(a,b):生成一个a到b包括a和b的整数
11，输出于输入：输出：数据小数点的格式输出：例如：f'{num:.2f}'意为保持2位小数的浮点数类型的num,注意搭配f使用
12，有趣的python：this模块：在python中输入import this就会看到《the zen of python》这篇python之禅
                 import antigravity：会打开一个网页漫画，画的是Python之父关于“import antigravity”的玩笑。
