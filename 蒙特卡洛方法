      蒙特卡洛方法是一种通过大量随机采样来近似求解确定性数学问题的数值计算方法，它的核心思想是：用随机模拟中事件发生的频率或样本统计量，来逼近问题的真实解。
一个比较容易理解这个方法的例子就是：当你要估算圆周率pi时,你可以这样做，画一个边长为1的正方形，然后画其内切圆，半径为0.5。之后随机往这个复合图形里面撒点，数一下
在圆内的点和在正方形里面的点，二者之比近似为圆周率。并且当撒点越多，这个值越接近圆周率
蒙特卡洛方法还能解决很多问题，它在代码上的实现可以看以下：
import random
import math
n = int(input())
SIM = 20000
count=0
is_end=True
total_count=0
for _ in range(SIM):     #循环SIM=20000次，也就是做了20000次随机试验
    x, y = 0,0
    while is_end:
        d = random.randint(1, 4)     #精华在此，用randint这个模块函数来随机生成1，2，3，4这四个数
        if d == 1:
            y += 1
        elif d == 2:
            y -= 1
        elif d == 3:
            x -= 1
        else:
            x += 1
        count+=1
        if x**2+y**2>n**2:         #做检验
            is_end=False
    total_count+=count        #把每次随机试验的结果汇总一起
    count=0
    x,y=0,0
    is_end=True
print(f'{(total_count/SIM):.0f}')     #求20000次随机试验结果的平均值，更精确
可以说，蒙特卡洛方法与random这个模块密不可分
